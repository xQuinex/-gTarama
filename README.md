KALI LINUX 

-

    sudo apt update
sudo apt full-upgrade -y
sudo apt autoremove -y

sudo apt install git python3 python3-pip python3-venv -y

git clone https://github.com/xQuinex/-gTarama.git
cd -gTarama

python3 -m venv venv
source venv/bin/activate

python3 "Dexron ~ paid2 ıg tool_ninjapy.py"
